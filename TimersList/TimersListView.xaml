<UserControl x:Class="TimersList.TimersListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:timer="clr-namespace:Timer;assembly=Timer"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="420"
             x:Name="Root">

	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="/Infrastructure;component/SharedResources/ResourceDictionary.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>

	<ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"
	              Background="{StaticResource LightBrush}">
		<WrapPanel x:Name="TimerWrapPanel">
			<timer:TimersListItemView Height="100" Margin="5"
			                          Width="{Binding ElementName=Root, Path=CtrlWidth, FallbackValue=200}" />
			<timer:TimersListItemView Height="100" Margin="5"
			                          Width="{Binding ElementName=Root, Path=CtrlWidth, FallbackValue=200}" />
			<timer:TimersListItemView Height="100" Margin="5"
			                          Width="{Binding ElementName=Root, Path=CtrlWidth, FallbackValue=200}" />
			<timer:TimersListItemView Height="100" Margin="5"
			                          Width="{Binding ElementName=Root, Path=CtrlWidth, FallbackValue=200}" />
			<timer:TimersListItemView Height="100" Margin="5"
			                          Width="{Binding ElementName=Root, Path=CtrlWidth, FallbackValue=200}" />
			<timer:TimersListItemView Height="100" Margin="5"
			                          Width="{Binding ElementName=Root, Path=CtrlWidth, FallbackValue=200}" />

		</WrapPanel>
	</ScrollViewer>
</UserControl>